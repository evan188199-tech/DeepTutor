## Decision Records

Lightweight ADR-style notes capturing *why* a feature was built, deferred, or
rejected. Each file is a single self-contained decision. Add new ones as
`<topic>.md`; do not rewrite history — supersede with a new file if a decision
changes.
